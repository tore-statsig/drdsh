import { NetworkCore, NetworkPriority } from '@statsig/client-core';
import { StatsigOptions } from './StatsigOptions';
export default class StatsigNetwork extends NetworkCore {
    private _downloadConfigSpecsUrlConfig;
    constructor(options?: StatsigOptions | null);
    fetchConfigSpecs(sdkKey: string, priority: NetworkPriority | undefined): Promise<string | null>;
}
